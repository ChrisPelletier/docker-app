version: "3"
services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    volumes:
      - ./api:/app
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
  db:
    image: 'mongo:latest'
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
networks:
  webnet: